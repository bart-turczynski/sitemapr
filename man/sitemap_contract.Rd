% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract-version.R
\name{sitemap_contract}
\alias{sitemap_contract}
\title{Published cross-port contract of this sitemapr build}
\usage{
sitemap_contract()
}
\value{
A named list:
\describe{
\item{\code{contract_id}}{Length-1 character. Stable identifier for the
findings-contract generation, e.g. \code{"sitemapr.findings/v2"}.}
\item{\code{contract_version}}{Length-1 character. The generation number.}
\item{\code{legacy_contract_version}}{Length-1 character. The older generation
still accepted by consumers.}
\item{\code{registry_revision}}{Length-1 character. Date-stamped revision of
\code{findings-registry.csv}.}
\item{\code{ruleset_revisions}}{Named character. Per-ruleset revisions; the
same values \code{\link[=ruleset_revision]{ruleset_revision()}} returns one at a time.}
\item{\code{sibling_versions}}{Named character. Supported version range per
sibling package.}
}
}
\description{
Returns the contract identity that sibling implementations pin against
(ADR-009 §7): which findings-contract generation this build speaks, which
revision of the finding-code registry it ships, the per-ruleset revisions,
and the sibling-package version ranges it is known to work with.
}
\details{
The \code{sibling_versions} map has the same shape as robotstxtr's
\code{robots_engine_contract_v1()$sibling_versions} and \code{sitemap-validator}'s
\code{SIBLING_VERSIONS}, so all three repos in the contract publish their ranges
identically.
}
\examples{
sitemap_contract()$sibling_versions
}
\seealso{
\code{\link[=ruleset_revision]{ruleset_revision()}} for a single ruleset's revision.
}
