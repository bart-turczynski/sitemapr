% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{sitemap_audit}
\alias{sitemap_audit}
\title{Assemble a sitemap audit result container}
\usage{
sitemap_audit(
  urls = NULL,
  findings = NULL,
  sources = NULL,
  problems = NULL,
  tree = NULL
)
}
\arguments{
\item{urls}{A \code{\link[=read_sitemap]{read_sitemap()}} tidy row tibble (optionally carrying the
\code{sources}/\code{problems} attributes). Defaults to the empty row schema.}

\item{findings}{A \code{\link[=validate_sitemap]{validate_sitemap()}} findings tibble. Defaults to the empty
findings contract.}

\item{sources}{The per-source fetch-metadata records. Defaults to the
\code{sources} attribute of \code{urls} when present, otherwise the empty schema.}

\item{problems}{The non-fatal parse \code{problems} companion table. Defaults to
the \code{problems} attribute of \code{urls} when present, else the empty schema.}

\item{tree}{A \code{\link[=sitemap_tree]{sitemap_tree()}} discovery/index structure. Defaults to the
empty tree schema.}
}
\value{
A validated \code{sitemap_audit} object: a classed list with the
components \code{urls}, \code{findings}, \code{sources}, \code{problems}, and \code{tree}. Access
them with \code{\link[=audit_urls]{audit_urls()}}, \code{\link[=audit_findings]{audit_findings()}}, \code{\link[=audit_sources]{audit_sources()}},
\code{\link[=audit_problems]{audit_problems()}}, and \code{\link[=audit_tree]{audit_tree()}}.
}
\description{
Bundles precomputed sitemapr outputs into a single validated \code{sitemap_audit}
object with stable, named components. It performs no fetching, parsing, or
validation of its own: it \emph{assembles} shapes the package already produces
(see \code{\link[=read_sitemap]{read_sitemap()}}, \code{\link[=validate_sitemap]{validate_sitemap()}}, and \code{\link[=sitemap_tree]{sitemap_tree()}}) into one
first-class result and validates their column contracts.
}
\details{
Because a \code{\link[=read_sitemap]{read_sitemap()}} result already carries its per-source metadata and
non-fatal parse problems as the \code{sources} and \code{problems} attributes, those
two components are pulled from \code{urls} when not passed explicitly, and then
promoted to first-class components (the stored \code{urls} component is the plain
tidy tibble, without those attributes). Any component left \code{NULL} defaults to
its empty (zero-row) schema, so \code{sitemap_audit()} with no arguments is a
valid empty audit and any subset of components yields a valid partial audit.
}
\examples{
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  '<url><loc>https://example.com/</loc>',
  '<lastmod>2024-01-01</lastmod></url>',
  '<url><loc>https://example.com/about</loc></url>',
  '</urlset>'
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)

# Assemble precomputed read + validate results into one container.
audit <- sitemap_audit(
  urls = read_sitemap(path),
  findings = validate_sitemap(path, mode = "non-strict")
)
audit
summary(audit)

# An empty audit is valid (all components are zero-row schemas).
sitemap_audit()
}
\seealso{
\code{\link[=read_sitemap]{read_sitemap()}}, \code{\link[=validate_sitemap]{validate_sitemap()}}, and \code{\link[=sitemap_tree]{sitemap_tree()}} for the
component producers.
}
