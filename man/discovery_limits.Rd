% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{discovery_limits}
\alias{discovery_limits}
\title{Default limits for discovery}
\usage{
discovery_limits(max_candidates = getOption("sitemapr.max_candidates", 50L))
}
\arguments{
\item{max_candidates}{Maximum number of distinct candidates to evaluate.
Resolves from the argument, then \code{getOption("sitemapr.max_candidates")},
then the default of 50.}
}
\value{
A named list of limits with coerced types.
}
\description{
Returns the configurable limits applied while discovering candidates. The
candidate cap bounds how many distinct guessed-path candidates are evaluated
against a single site root (after deduplication), so a large catalog can
never trigger an unbounded burst of requests.
}
\examples{
discovery_limits()
discovery_limits(max_candidates = 10)
}
\seealso{
\code{\link[=fetch_limits]{fetch_limits()}} and \code{\link[=index_limits]{index_limits()}} for the other bound
constructors, and \code{\link[=sitemap_tree]{sitemap_tree()}} which accepts discovery limits.
}
