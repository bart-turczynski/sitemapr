% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ruleset-context.R
\name{ruleset_context}
\alias{ruleset_context}
\title{Construct a per-source validation context (ADR-009 §1, §12.1)}
\usage{
ruleset_context(
  submission_channel = "absent",
  discovery_provenance = "organic",
  property_scope = NA_character_,
  authority_evidence = "absent"
)
}
\arguments{
\item{submission_channel}{One of \code{search_console_api}, \code{webmaster_tools},
\code{user}, \code{import}, or \code{absent} (default). \code{"discovered"} is rejected.}

\item{discovery_provenance}{One of \code{organic} (default),
\code{robots_txt_reference}, \code{supplied}, \code{guessed_path}, \code{index_child}, or
\code{archive_derived}.}

\item{property_scope}{The verified property/site string (a URL) or \code{NA}
(default).}

\item{authority_evidence}{One of \code{absent} (default), \code{verified_property_set},
\code{target_host_robots_reference}, \code{same_location_default}, or \code{conflicting}.}
}
\value{
An object of class \code{sitemapr_ruleset_context}: a named list of the
four axes, the shape the findings \code{context} list-column carries
(\code{docs/findings-contract.md}).
}
\description{
Bundles the four \strong{independent} per-source context axes into a validated
object. There is deliberately no single \code{profile} scalar (ADR-009 §1);
\code{profile} stays reserved for the XSD schema-profile sense. Each axis carries
its own exact, extensible value set (\code{sitemapr} owns them):
}
\details{
\itemize{
\item \code{submission_channel} — how the artifact was submitted. \code{"discovered"} is
\strong{not} a submission value and is rejected; a child that inherits no
submission facts is \code{"absent"}.
\item \code{discovery_provenance} — how the artifact was found. A robots.txt reference
used as cross-site trust is distinct from robots.txt \emph{discovery}.
\item \code{property_scope} — the verified property/site a submission is bound to (a
URL/site string or \code{NA}); it does not by itself grant authority beyond that
property.
\item \code{authority_evidence} — \strong{structured, never boolean}: the kind of evidence
that establishes cross-site authority for this source.
}

These are \strong{per-source} facts: a sitemap-index child inherits nothing
implicitly (§12.1). Build a child's context from its own evidence with
\code{\link[=ruleset_context_for_child]{ruleset_context_for_child()}}, which takes no parent context. This
constructor is the pure data layer; it does not evaluate scope or authority.
}
\examples{
ruleset_context()
ruleset_context(
  submission_channel = "search_console_api",
  property_scope = "https://example.com/",
  authority_evidence = "verified_property_set"
)
}
\seealso{
\code{\link[=ruleset_context_for_child]{ruleset_context_for_child()}} for the per-source invariant helper
and \code{\link[=sitemap_rulesets]{sitemap_rulesets()}} for the engine axis.
}
