% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robots-context.R
\name{robots_context}
\alias{robots_context}
\title{Construct a robots evaluation context (ADR-009 §1, sitemap-spec §13.0)}
\usage{
robots_context(
  product_token = "*",
  policy_ruleset = "google",
  matcher_backend = "google"
)
}
\arguments{
\item{product_token}{The robots.txt group to match against; defaults to the
catch-all \code{"*"}.}

\item{policy_ruleset}{The robots status-policy ruleset; defaults to
\code{"google"}, the historical behavior of \code{validate_sitemap()}.}

\item{matcher_backend}{The robots matcher backend; defaults to \code{"google"}.}
}
\value{
An object of class \code{sitemapr_robots_context}: a named list of the
three axes.
}
\description{
Bundles the three \strong{independent} robots axes one allow/disallow evaluation
runs under. It is the carrier \code{\link[=validate_sitemap_robots]{validate_sitemap_robots()}} accepts, and it is
deliberately separate from \code{\link[=ruleset_context]{ruleset_context()}}: selecting a sitemap ruleset
does not select a robots policy (ADR-009 §1). A Bing \emph{sitemap} ruleset does
not imply a Bing \emph{robots} policy — the two are chosen independently.
}
\details{
\itemize{
\item \code{product_token} — the robots.txt group used for \strong{matching}: \code{"*"} for
the catch-all group, or a crawler token such as \code{"Googlebot"}. This is not
the HTTP request user-agent sitemapr fetches with.
\item \code{policy_ruleset} — whose HTTP-status → policy semantics govern (what a
404, a 403 or a 5xx robots.txt \emph{means}).
\item \code{matcher_backend} — which matcher decides a rule against a URL.
}

The vocabulary for the last two belongs to the sibling \code{robotstxtr} package,
not to sitemapr, and each value is validated against the set the
\strong{installed} build publishes on its engine contract — so an axis this
build cannot honour is rejected here rather than deep inside the engine.
\code{robotstxtr} is an optional dependency; when it is absent only the shape of
each axis is checked.

A backend whose published \code{token_policy} is \code{bounded_profiles} (Bing,
Yandex) accepts only its own vendor profile tokens, and the accepted set is
not published, so it cannot be validated here. An unsupported token is not an
error: every URL comes back undecided and surfaces as \code{ROBOTS_INDETERMINATE}.
Prefer \code{\link[=robots_context_preset]{robots_context_preset()}}, whose tokens are known-good for their
backend.
}
\examples{
robots_context()
robots_context(product_token = "Googlebot")
}
\seealso{
\code{\link[=robots_context_preset]{robots_context_preset()}} for the per-engine presets,
\code{\link[=validate_sitemap_robots]{validate_sitemap_robots()}} for the entry point that accepts one, and
\code{\link[=ruleset_context]{ruleset_context()}} for the independent sitemap-source axes.
}
