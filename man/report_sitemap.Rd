% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report_sitemap}
\alias{report_sitemap}
\title{Render a self-contained HTML report for a sitemap}
\usage{
report_sitemap(
  x,
  output = NULL,
  mode = c("strict", "non-strict"),
  urls = NULL,
  findings = NULL,
  title = NULL,
  user_agent = default_user_agent(),
  limits = fetch_limits(),
  index_limits = NULL
)
}
\arguments{
\item{x}{A single source: a sitemap URL (character) or a path to a
local sitemap file. When both \code{urls} and \code{findings} are supplied, \code{x} is
used only as the displayed source label.}

\item{output}{Optional path to write the HTML file to. When supplied, the
report is written there (UTF-8) and the path is returned invisibly;
otherwise the HTML is returned as an \link[htmltools:HTML]{htmltools::HTML} string.}

\item{mode}{\code{"strict"} (the default) or \code{"non-strict"}, passed to
\code{\link[=validate_sitemap]{validate_sitemap()}} when \code{findings} is not supplied.}

\item{urls}{Optional precomputed \code{\link[=read_sitemap]{read_sitemap()}} result (a tibble with the
\code{sources} attribute). When \code{NULL} (the default) it is computed from \code{x}.}

\item{findings}{Optional precomputed \code{\link[=validate_sitemap]{validate_sitemap()}} findings tibble.
When \code{NULL} (the default) it is computed from \code{x}.}

\item{title}{The HTML document \verb{<title>}. Defaults to a title derived from
\code{x}.}

\item{user_agent}{The User-Agent header for HTTP fetches. Defaults to the
package User-Agent.}

\item{limits}{Network limits for HTTP fetches, as from \code{fetch_limits()}.}

\item{index_limits}{Sitemapindex-expansion bounds, as from \code{index_limits()}.
Defaults to \code{index_limits()}.}
}
\value{
If \code{output} is supplied, the output path, invisibly. Otherwise, the
report HTML as an \link[htmltools:HTML]{htmltools::HTML} character string.
}
\description{
Produces a single, fully self-contained HTML report for a sitemap source,
modeled on the sitemap-validator reference renderer. Unlike \code{\link[=read_sitemap]{read_sitemap()}}
(which returns a tidy tibble) and \code{\link[=validate_sitemap]{validate_sitemap()}} (which returns the
findings contract), \code{report_sitemap()} is the human-readable surface: it
\emph{consumes} those existing outputs and renders them, and never re-implements
any parsing or validation itself.
}
\details{
The report contains a hero banner (source, overall status, URL/index/sitemap
counts), a per-source sitemap table (format, HTTP status, URL count, and
\code{lastmod}/\code{priority}/\code{changefreq} presence), \code{lastmod} coverage cards with a
by-month histogram, a collapsible URL folder tree grouped by path segment, a
severity dashboard, the findings grouped by validation layer (deduplicated by
code, with evidence excerpts), and a searchable, sortable, CSV-exportable URL
table.

The output is entirely self-contained: all CSS, JavaScript (search, sort,
CSV export, tree toggle, and a light/dark theme toggle), and data are
inlined, so the file references no external hosts and works offline. The
palette follows the viewer's \code{prefers-color-scheme} by default; the in-page
toggle stamps a \code{data-theme} attribute on the root element that wins in both
directions.

By default the source \code{x} is both read (via \code{\link[=read_sitemap]{read_sitemap()}}, for the URL
rows and per-source metadata) and validated (via \code{\link[=validate_sitemap]{validate_sitemap()}}, for
the findings). To avoid re-fetching a URL source, or to render results you
have already computed, pass them via \code{urls} and/or \code{findings}; in that case
\code{x} is used only as the report's source label.
}
\examples{
# Render a report for a local sitemap file to a temporary HTML file.
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  '<url><loc>https://example.com/</loc>',
  '<lastmod>2024-01-01</lastmod></url>',
  '<url><loc>https://example.com/about</loc></url>',
  '</urlset>'
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)
out <- tempfile(fileext = ".html")
report_sitemap(path, output = out)

# Render directly from a sitemap URL (fetches twice: read + validate).
# report_sitemap("https://example.com/sitemap.xml", output = "report.html")

# Reuse results you have already computed to avoid re-fetching.
# u <- read_sitemap("https://example.com/sitemap.xml")
# f <- validate_sitemap("https://example.com/sitemap.xml")
# report_sitemap(
#   "example.com", urls = u, findings = f, output = "report.html"
# )
}
\seealso{
\code{\link[=read_sitemap]{read_sitemap()}} and \code{\link[=validate_sitemap]{validate_sitemap()}} for the underlying data.
}
