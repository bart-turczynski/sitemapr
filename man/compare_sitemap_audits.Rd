% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-compare.R
\name{compare_sitemap_audits}
\alias{compare_sitemap_audits}
\alias{print.sitemap_audit_diff}
\alias{summary.sitemap_audit_diff}
\title{Compare two sitemap audits}
\usage{
compare_sitemap_audits(old, new)

\method{print}{sitemap_audit_diff}(x, ...)

\method{summary}{sitemap_audit_diff}(object, ...)
}
\arguments{
\item{old, new}{\code{sitemap_audit} objects (the baseline and the new capture).}

\item{x, object, ...}{A \code{sitemap_audit_diff} object and ignored extra
arguments, for the \code{print()}/\code{summary()} methods.}
}
\value{
A \code{sitemap_audit_diff}: a classed list whose \code{components} element
holds, for each of \code{urls}, \code{findings}, \code{sources}, \code{problems}, and \code{tree}, a
list of \code{added}, \code{removed}, and \code{changed} tibbles (each carrying the
component's columns, deterministically ordered by key). Use
\code{\link[=audit_unchanged]{audit_unchanged()}} to test whether anything changed, and \code{summary()} for a
per-component count table.
}
\description{
Deterministically diffs two \code{\link[=sitemap_audit]{sitemap_audit()}} objects component-by-component
and reports which rows were added, removed, or changed. Built for CI drift
monitoring (e.g. a scheduled job that flags when a site's sitemap audit
changes): comparing an audit to an identically-recomputed copy of itself
always yields an empty diff.
}
\details{
Rows are matched by a stable identity key, never by position, so shuffling a
component's rows never produces a diff. Keys are: \code{loc} for \code{urls};
\code{requested_url} for \code{sources}; \code{sitemap_url} + \code{parent_sitemap} + \code{depth} for
\code{tree}; and the whole row for \code{findings} and \code{problems} (a finding present in
one audit and not the other is added/removed).

Change detection ignores run-varying metadata so re-running the same audit
does not show spurious diffs: the elapsed-fetch column \code{sources$timing} is
excluded from the comparison. All other columns are deterministic content and
are compared.
}
\examples{
base <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  "<url><loc>https://example.com/</loc></url>",
  "<url><loc>https://example.com/a</loc></url></urlset>"
)
moved <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  "<url><loc>https://example.com/</loc></url>",
  "<url><loc>https://example.com/b</loc></url></urlset>"
)
p1 <- tempfile(fileext = ".xml")
p2 <- tempfile(fileext = ".xml")
writeLines(base, p1)
writeLines(moved, p2)

old <- sitemap_audit(urls = read_sitemap(p1))
new <- sitemap_audit(urls = read_sitemap(p2))
d <- compare_sitemap_audits(old, new)
d
summary(d)

# An audit compared with itself has an empty diff.
audit_unchanged(compare_sitemap_audits(old, old))
}
\seealso{
\code{\link[=sitemap_audit]{sitemap_audit()}} for the compared container, and
\code{\link[=audit_unchanged]{audit_unchanged()}} for the "did anything change?" predicate.
}
