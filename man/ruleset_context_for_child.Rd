% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ruleset-context.R
\name{ruleset_context_for_child}
\alias{ruleset_context_for_child}
\title{Per-source context for a sitemap-index child (ADR-009 §1, §12.1 invariant)}
\usage{
ruleset_context_for_child(
  submission_channel = "absent",
  discovery_provenance = "index_child",
  property_scope = NA_character_,
  authority_evidence = "absent"
)
}
\arguments{
\item{submission_channel}{One of \code{search_console_api}, \code{webmaster_tools},
\code{user}, \code{import}, or \code{absent} (default). \code{"discovered"} is rejected.}

\item{discovery_provenance}{One of \code{organic} (default),
\code{robots_txt_reference}, \code{supplied}, \code{guessed_path}, \code{index_child}, or
\code{archive_derived}.}

\item{property_scope}{The verified property/site string (a URL) or \code{NA}
(default).}

\item{authority_evidence}{One of \code{absent} (default), \code{verified_property_set},
\code{target_host_robots_reference}, \code{same_location_default}, or \code{conflicting}.}
}
\value{
An object of class \code{sitemapr_ruleset_context} built solely from the
child's own evidence.
}
\description{
Expresses the per-source invariant that \strong{a sitemap-index child inherits
nothing implicitly}: this helper takes \strong{no parent context}, so a child's
context can only be established by its own evidence. The defaults encode a
child with no submission facts (\code{submission_channel = "absent"}), found as an
index child (\code{discovery_provenance = "index_child"}), no bound property, and
no authority evidence (\code{authority_evidence = "absent"}). A caller supplies
the child's own evidence via the arguments.
}
\details{
This is the invariant-respecting context primitive only; it does \strong{not}
evaluate scope or authority (§12.2; S4/S5 own those). Because there is no
parent parameter, a submitted or verified parent index cannot confer scope or
authority on a child by construction.
}
\examples{
# A child with no evidence of its own: nothing is inherited from the parent.
ruleset_context_for_child()

# A child that carries its own verified-property evidence.
ruleset_context_for_child(
  property_scope = "https://cdn.example.com/",
  authority_evidence = "verified_property_set"
)
}
\seealso{
\code{\link[=ruleset_context]{ruleset_context()}} for the general constructor.
}
