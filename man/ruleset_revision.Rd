% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ruleset-context.R
\name{ruleset_revision}
\alias{ruleset_revision}
\title{Published revision of a \code{sitemap_ruleset}}
\usage{
ruleset_revision(ruleset = sitemap_rulesets())
}
\arguments{
\item{ruleset}{A single \code{sitemap_ruleset} name; see \code{\link[=sitemap_rulesets]{sitemap_rulesets()}}.
Defaults to the baseline \code{"sitemaps.org"}.}
}
\value{
A length-1 character revision string.
}
\description{
Returns the published revision string for a ruleset (ADR-009 §1/§7) so
cross-repo consumers can pin a compatible version. The revisions live in one
documented constant map (a single source of truth); a ruleset's revision
advances only when that ruleset's own rules change.
}
\examples{
ruleset_revision()
ruleset_revision("yandex")
}
\seealso{
\code{\link[=sitemap_rulesets]{sitemap_rulesets()}} for the value set.
}
