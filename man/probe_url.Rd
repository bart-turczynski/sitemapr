% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probe.R
\name{probe_url}
\alias{probe_url}
\title{Probe a URL to inspect what it is, without resolving it}
\usage{
probe_url(
  url,
  limits = fetch_limits(),
  user_agent = default_user_agent(),
  ssrf_guard = TRUE,
  policy = request_policy()
)
}
\arguments{
\item{url}{A single sitemap URL or local file path to inspect.}

\item{limits}{Network limits for the fetch, as from \code{\link[=fetch_limits]{fetch_limits()}}.}

\item{user_agent}{The User-Agent header for the fetch. Defaults to the
package User-Agent.}

\item{ssrf_guard}{Logical; when \code{TRUE} (default) the structural SSRF guard
runs on every hop.}

\item{policy}{A request policy applied to the fetch, as from
\code{request_policy()}. Defaults to the no-op policy.}
}
\value{
A \code{sitemapr_probe} object: a classed list with the fields \code{url},
\code{final_url}, \code{status_code}, \code{content_type}, \code{detected_type}, \code{xml_root},
\code{is_compressed}, \code{child_count}, \code{sample}, \code{problems}, and \code{suggested_next}.
\code{detected_type} is one of \code{"sitemap"}, \code{"sitemap_index"}, \code{"feed"},
\code{"robots_txt"}, \code{"html"}, \code{"xml_other"}, \code{"not_found"}, \code{"fetch_error"},
or \code{"parse_error"}.
}
\description{
\code{probe_url()} is a lightweight, \strong{non-resolving} inspection: it fetches
exactly one URL (or reads one local file) and reports what it is — a sitemap,
a sitemap index, a feed, robots.txt, an HTML page, or an error state —
\strong{without} expanding indexes or following children. It is the diagnostic
counterpart to \code{\link[=read_sitemap]{read_sitemap()}}, which resolves a sitemap to its final URL
rows. Use \code{probe_url()} to diagnose first and \code{\link[=read_sitemap]{read_sitemap()}} to resolve.
}
\details{
The fetch goes through the same bounded, SSRF-safe engine as the rest of the
package, so \code{probe_url()} inherits the network-safety policy (per-hop SSRF
guard, redirect and body-size ceilings) and the \code{limits} / \code{policy}
overrides. It fetches \strong{only} the given URL: for a sitemap index it \emph{counts}
the direct \verb{<sitemap>} children by parsing the index locally, but it never
fetches them.

A fetch or parse failure is represented in the returned record
(\code{detected_type} becomes \code{"fetch_error"}, \code{"not_found"}, or \code{"parse_error"},
with an explanatory \code{problems} row), not thrown — \code{probe_url()} is
diagnostic. Only invalid input (a non-string \code{url}) raises an error.
}
\examples{
# Probe a local sitemap file: detected as a urlset, with a child count.
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  "<url><loc>https://example.com/</loc></url>",
  "<url><loc>https://example.com/about</loc></url>",
  "</urlset>"
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)
probe_url(path)

# A sitemap index: children are COUNTED, never fetched.
index <- paste0(
  '<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  "<sitemap><loc>https://example.com/s1.xml</loc></sitemap>",
  "<sitemap><loc>https://example.com/s2.xml</loc></sitemap>",
  "</sitemapindex>"
)
index_path <- tempfile(fileext = ".xml")
writeLines(index, index_path)
probe_url(index_path)

# Probe a live URL (requires network):
# probe_url("https://example.com/sitemap.xml")
}
\seealso{
\code{\link[=read_sitemap]{read_sitemap()}} to resolve a sitemap to its URL rows, and
\code{\link[=sitemap_tree]{sitemap_tree()}} to discover a site's sitemaps from its root.
}
