% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robots-context.R
\name{robots_context_preset}
\alias{robots_context_preset}
\title{Per-engine robots context preset (sitemap-spec §13.0)}
\usage{
robots_context_preset(preset = robots_context_presets())
}
\arguments{
\item{preset}{A single preset name; see \code{\link[=robots_context_presets]{robots_context_presets()}}. Defaults
to \code{"google"}.}
}
\value{
An object of class \code{sitemapr_robots_context}, the same shape
\code{\link[=robots_context]{robots_context()}} returns, carrying the expanded axis values plus the
\code{preset} name.
}
\description{
A construction-time preset selecting one engine's robots product token,
status policy and matcher together, so a caller need not know which token a
bounded matcher backend accepts. It is a thin wrapper over
\code{\link[=robots_context]{robots_context()}}; every axis stays independently overridable by calling
that constructor directly.
}
\details{
The preset's values are \strong{expanded onto the returned context} rather than
re-derived at use time, so the result records exactly which product token,
policy ruleset and matcher backend the preset selected. The name is retained
on \verb{$preset} for provenance.

A preset selects a robots engine only. It does \strong{not} select a sitemap
ruleset, and no sitemap ruleset selects it (ADR-009 §1): the bridge between
the two value sets is this documented preset, never a silent derivation.

Not every preset is runnable on every install. \code{robotstxtr} publishes a
\code{matcher_availability} per backend, and one reporting
\code{capability_unavailable} decides nothing — a context on it evaluates
cleanly but reports every URL as \code{ROBOTS_INDETERMINATE} rather than
guessing.
}
\examples{
robots_context_preset("google")

# The EXPANDED values are readable back off the context.
yandex <- robots_context_preset("yandex")
yandex$product_token
yandex$matcher_backend
}
\seealso{
\code{\link[=robots_context]{robots_context()}} for the general constructor and
\code{\link[=validate_sitemap_robots]{validate_sitemap_robots()}} for the entry point that accepts the result.
}
