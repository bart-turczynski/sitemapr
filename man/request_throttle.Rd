% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{request_throttle}
\alias{request_throttle}
\title{Host-aware request-throttle settings for a request-policy}
\usage{
request_throttle(min_interval = NULL, requests = NULL, window = NULL)
}
\arguments{
\item{min_interval}{Minimum seconds between consecutive requests to one host.}

\item{requests, window}{A request budget: at most \code{requests} requests per
\code{window} seconds to one host, evenly paced (interval \code{window / requests}).
Both must be supplied together and take precedence over \code{min_interval}.}
}
\value{
A \code{sitemapr_request_throttle} for \code{request_policy(throttle = )}.
}
\description{
Configures per-host request pacing for a request-policy. Pass the result as
\code{request_policy(throttle = )}. Requests are keyed by canonical host:port, so
different origins pace independently and a redirect onto another host pays
that host's pace, not the origin's. Supply EITHER a minimum interval between
consecutive requests to one host (\code{min_interval}, seconds) OR a request
budget per time window (\code{requests} per \code{window} seconds, evenly paced at
\code{window / requests}). Pacing is shared across every hop of one logical
operation (roots, redirects, robots, discovery candidates, and index
children), so an index traversal's many children pace against one set of host
buckets. \code{NULL} (the default policy field) means no pacing.
}
\examples{
# At most one request per host every 2 seconds.
request_throttle(min_interval = 2)

# A budget of 10 requests per 60 seconds per host (evenly paced).
request_throttle(requests = 10, window = 60)
}
\seealso{
Other request-policy:
\code{\link[=request_auth_basic]{request_auth_basic()}},
\code{\link[=request_auth_bearer]{request_auth_bearer()}},
\code{\link[=request_policy]{request_policy()}},
\code{\link[=request_proxy]{request_proxy()}},
\code{\link[=request_retry]{request_retry()}}
}
\concept{request-policy}
