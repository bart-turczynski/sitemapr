% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seed.R
\name{sitemap_tree_from_bytes}
\alias{sitemap_tree_from_bytes}
\title{Discover a site's sitemaps from already-fetched bytes}
\usage{
sitemap_tree_from_bytes(
  bytes,
  source_url,
  user_agent = default_user_agent(),
  net_limits = fetch_limits(),
  index_limits = NULL
)
}
\arguments{
\item{bytes}{The raw sitemap document: a raw vector, or a length-1 character
string of the sitemap text. Transparent gzip is inflated automatically.}

\item{source_url}{The URL the bytes came from (length-1 character). Used as
the root row's \code{sitemap_url}, as the cycle-detection identity for
expansion, and as the parent attribution for expanded children.}

\item{user_agent}{The User-Agent header for the child fetches an index seed
triggers. Defaults to the package User-Agent.}

\item{net_limits}{Network limits for the child fetches, as from
\code{fetch_limits()}.}

\item{index_limits}{Sitemapindex-expansion bounds, as from \code{index_limits()}.
Defaults to \code{index_limits()}.}
}
\value{
A tibble with the same 8-column contract as \code{\link[=sitemap_tree]{sitemap_tree()}}:
\code{depth}, \code{parent_sitemap}, \code{sitemap_url}, \code{page_count}, \code{gzip}, \code{status},
\code{reason}, and \code{provenance}.
}
\description{
Builds the same discovery tree as \code{\link[=sitemap_tree]{sitemap_tree()}}, but from sitemap bytes
you have already fetched yourself rather than from a site root. This is the
explicit-seed entry point for sources \code{sitemap_tree()} cannot fetch directly
— for example a site behind a TLS-fingerprint bot wall, whose sitemap you
retrieved through a browser or proxy. The root document is parsed in memory
(no network request for it); if it is a \code{sitemapindex}, its child sitemaps
are still fetched and expanded over the network, cycle-safe and depth- and
count-capped, exactly as \code{\link[=sitemap_tree]{sitemap_tree()}} and \code{\link[=read_sitemap]{read_sitemap()}} do.
}
\details{
A leaf \code{urlset}/text seed performs no network access at all. The root row
carries \code{provenance = "seed"}; expansion children carry \code{"child-of-index"}.
Content that cannot be parsed yields a single \code{rejected} seed row rather than
an error.
}
\examples{
# Parse an already-fetched leaf sitemap with no network access.
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  "<url><loc>https://example.com/</loc></url>",
  "<url><loc>https://example.com/about</loc></url>",
  "</urlset>"
)
sitemap_tree_from_bytes(xml, source_url = "https://example.com/sitemap.xml")
}
\seealso{
\code{\link[=sitemap_tree]{sitemap_tree()}} for discovery from a site root or an exact URL.
}
