% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sitemapr-package.R
\docType{package}
\name{sitemapr-package}
\alias{sitemapr}
\alias{sitemapr-package}
\title{sitemapr: Parse and Validate XML Sitemaps}
\description{
A deterministic toolkit for reading and validating XML, text, and index sitemaps against the Sitemap Protocol 0.9 and related W3C and RFC standards. It parses sitemap sources into tidy tibbles, expands sitemap indexes, and reports schema and protocol findings with stable, reproducible output.
}
\examples{
# A short offline tour; no call below performs a network request.

# A small sitemap with two deliberate problems: a priority outside [0, 1]
# and a date-only lastmod.
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  '<url><loc>https://example.com/</loc>',
  '<lastmod>2024-01-01</lastmod><priority>0.8</priority></url>',
  '<url><loc>https://example.com/about</loc>',
  '<priority>7</priority></url>',
  '</urlset>'
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)

# 1. Read the source into one tidy row per URL. Extension elements arrive
# in the images/video/news/alternates list-columns.
read_sitemap(path)

# 2. Validate it: one row per issue, each with a stable code, a severity,
# and the layer that produced it. The same source always yields a
# row-for-row identical result.
validate_sitemap(path)[, c("code", "severity", "layer")]

# 3. Audit in a single pass, then reach into the components.
audit <- audit_sitemap(path)
audit_findings(audit)$code
nrow(audit_urls(audit))

# 4. Engine-aware validation. Each supported ruleset interprets the same
# source under its own published rules, and the findings carry the
# ruleset and its revision.
sitemap_rulesets()
ruleset_revision("google")
google <- validate_sitemap_ruleset(path, sitemap_ruleset = "google")
google[, c("code", "severity", "ruleset")]

# 5. The default bounds every fetch and index expansion runs under.
fetch_limits()
index_limits()
}
\seealso{
Reading: \code{\link[=read_sitemap]{read_sitemap()}}.
Discovery: \code{\link[=sitemap_tree]{sitemap_tree()}}, \code{\link[=sitemap_tree_from_bytes]{sitemap_tree_from_bytes()}}, \code{\link[=probe_url]{probe_url()}}.
Validation: \code{\link[=validate_sitemap]{validate_sitemap()}}, \code{\link[=validate_sitemap_ruleset]{validate_sitemap_ruleset()}}.
Rulesets and per-source context: \code{\link[=sitemap_rulesets]{sitemap_rulesets()}},
\code{\link[=ruleset_revision]{ruleset_revision()}}, \code{\link[=ruleset_context]{ruleset_context()}}, \code{\link[=ruleset_context_for_child]{ruleset_context_for_child()}},
\code{\link[=gsc_submission]{gsc_submission()}}, \code{\link[=robots_cross_submission]{robots_cross_submission()}}.
Auditing: \code{\link[=audit_sitemap]{audit_sitemap()}}, \code{\link[=sitemap_audit]{sitemap_audit()}}, \code{\link[=compare_sitemap_audits]{compare_sitemap_audits()}},
\code{\link[=audit_unchanged]{audit_unchanged()}}, \code{\link[=audit_accessors]{audit_accessors()}}, \code{\link[=sitemap_companions]{sitemap_companions()}}.
Limits: \code{\link[=discovery_limits]{discovery_limits()}}, \code{\link[=index_limits]{index_limits()}}, \code{\link[=fetch_limits]{fetch_limits()}}.
Reporting: \code{\link[=report_sitemap]{report_sitemap()}}.
Request customization: \code{\link[=request_policy]{request_policy()}}, \code{\link[=request_auth_basic]{request_auth_basic()}},
\code{\link[=request_auth_bearer]{request_auth_bearer()}}, \code{\link[=request_proxy]{request_proxy()}}, \code{\link[=request_retry]{request_retry()}},
\code{\link[=request_throttle]{request_throttle()}}.

The \code{introduction} vignette is a full tour:
\code{vignette("introduction", package = "sitemapr")}.

URL parsing and canonicalization are delegated to the \pkg{rurl} package,
robots.txt evaluation to \pkg{robotstxtr}, and HTTP is performed by
\pkg{httr2}.
}
\author{
\strong{Maintainer}: Bart Turczynski \email{bartek@turczynski.pl} (\href{https://orcid.org/0000-0002-8788-7980}{ORCID})

Authors:
\itemize{
  \item Bart Turczynski \email{bartek@turczynski.pl} (\href{https://orcid.org/0000-0002-8788-7980}{ORCID})
}

}
\keyword{internal}
