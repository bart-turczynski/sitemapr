% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-compare.R
\name{audit_unchanged}
\alias{audit_unchanged}
\title{Did a sitemap-audit comparison find any change?}
\usage{
audit_unchanged(x)
}
\arguments{
\item{x}{A \code{sitemap_audit_diff}, as returned by \code{\link[=compare_sitemap_audits]{compare_sitemap_audits()}}.}
}
\value{
A length-one logical: \code{TRUE} if no rows were added, removed, or
changed in any component, else \code{FALSE}.
}
\description{
The "did anything change?" predicate for a \code{\link[=compare_sitemap_audits]{compare_sitemap_audits()}} result:
\code{TRUE} when the two audits are identical (an empty diff), \code{FALSE} otherwise.
}
\examples{
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  "<url><loc>https://example.com/</loc></url></urlset>"
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)
audit <- sitemap_audit(urls = read_sitemap(path))

audit_unchanged(compare_sitemap_audits(audit, audit))
}
\seealso{
\code{\link[=compare_sitemap_audits]{compare_sitemap_audits()}}.
}
