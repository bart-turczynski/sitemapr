% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{request_policy}
\alias{request_policy}
\title{Construct a request-policy for the fetch boundary}
\usage{
request_policy(
  prepare = NULL,
  headers = NULL,
  auth = NULL,
  proxy = NULL,
  tls = NULL,
  retry = NULL,
  throttle = NULL,
  max_active = NULL
)
}
\arguments{
\item{prepare}{Optional \verb{function(req, ctx)} receiving the built httr2
request plus a hop-context list (currently the resolved hop \code{url}) and
returning the possibly-modified request. Applied last of the caller-facing
steps, so it composes on top of the typed fields.}

\item{headers}{Optional named list or named character vector of HTTP headers
to add via \code{httr2::req_headers()}.}

\item{auth}{Optional authentication object from \code{\link[=request_auth_basic]{request_auth_basic()}} or
\code{\link[=request_auth_bearer]{request_auth_bearer()}}, applied via \code{httr2::req_auth_basic()} /
\code{httr2::req_auth_bearer_token()}.}

\item{proxy}{Optional proxy: either a proxy URL string or a
\code{\link[=request_proxy]{request_proxy()}} object, applied via \code{httr2::req_proxy()}.}

\item{tls}{Optional named list of curl/TLS options (e.g.
\code{list(ssl_verifypeer = 0L)}) passed through \code{httr2::req_options()}. Cannot
override sitemapr's redirect controls, which are re-asserted afterwards.}

\item{retry}{Optional \code{\link[=request_retry]{request_retry()}} object enabling bounded retry with
exponential backoff. \code{NULL} (the default) means a single attempt per hop —
byte-identical to the pre-retry behavior. When set, only TRANSIENT failures
retry: the retryable HTTP status set (default \verb{429, 500, 502, 503, 504})
and, when \code{retry_on_failure = TRUE}, transient transport errors. Retries
happen inside a single hop's \code{req_perform()}, so they never inflate the
redirect/hop count or consume the redirect budget. Deterministic failures
are NEVER retried: SSRF rejections (raised before the request), malformed
input (classified downstream), and resource-ceiling aborts (raised after
the response is buffered) all sit outside the retried code path. Any
\code{Retry-After} header is honored but BOUNDED by the configured max backoff.}

\item{throttle}{Optional \code{\link[=request_throttle]{request_throttle()}} object enabling host-aware
request pacing. \code{NULL} (the default) means no pacing, byte-identical to
the pre-throttle behavior. When set, requests are keyed by canonical
host:port, so different origins pace independently and a redirect onto
another host pays that host's pace, not the origin's. Pacing runs around
each request (after the per-hop SSRF guard and sitemapr's re-asserted
transport controls), so it never weakens those safety semantics.}

\item{max_active}{Optional worker cap enabling opt-in bounded-concurrency
expansion of a sitemap index (ADR-008). \code{NULL} (the default) keeps child
fetches sequential — byte-identical to the pre-concurrency path — and
\code{max_active = 1} is observably identical to that default. A larger value
(a small, polite bound such as \code{4}–\code{6} is typical) fetches up to that many
independent child sitemaps at once while the per-host \code{throttle} still
paces each origin. Concurrency changes only \emph{when} a child's bytes arrive,
never the row/finding/tree order or the budget-truncation point: the output
is byte-identical to sequential mode.}
}
\value{
An object of class \code{sitemapr_request_policy}.
}
\description{
A request-policy is the single, constrained extension seam for customizing
the httr2 requests sitemapr issues for every hop (root, redirect, discovery,
robots, and index-child requests). It carries typed fields for custom HTTP
headers, authentication, a proxy, and TLS/curl options, plus a generic
\code{prepare} hook for anything not covered by the typed fields. Every field
defaults to \code{NULL}, so the default policy is a byte-identical no-op and
existing callers behave exactly as before.
}
\details{
\strong{What callers may set.} \code{headers}, \code{auth}, \code{proxy}, \code{tls}, and \code{prepare}
are all applied to each hop's request AFTER the per-hop SSRF guard and
BEFORE sitemapr asserts its own transport controls.

\strong{What sitemapr always owns (callers cannot override).} Redirect control
(\code{followlocation = 0}, \code{maxredirs = 0}) and the non-2xx error policy are
re-applied \emph{after} all caller customization, and the
structural SSRF guard re-runs on every redirect hop before any network call.
A policy therefore cannot re-enable automatic redirect following, defeat the
per-hop SSRF re-check, or turn a non-2xx status into a transport error — even
via \code{tls}/\code{prepare} — because sitemapr's controls win last.
}
\examples{
# (1) A custom header (e.g. to satisfy a staging gateway).
request_policy(headers = list("X-Env" = "staging"))

# (2) Basic and bearer authentication.
request_policy(auth = request_auth_basic("user", "secret"))
request_policy(auth = request_auth_bearer("a-token"))

# (3) A corporate proxy.
request_policy(proxy = "http://proxy.internal:3128")
request_policy(proxy = request_proxy("proxy.internal", port = 3128))

# (4) Bounded-concurrency index expansion (opt-in; sequential by default).
request_policy(max_active = 6)
}
\seealso{
Other request-policy:
\code{\link[=request_auth_basic]{request_auth_basic()}},
\code{\link[=request_auth_bearer]{request_auth_bearer()}},
\code{\link[=request_proxy]{request_proxy()}},
\code{\link[=request_retry]{request_retry()}},
\code{\link[=request_throttle]{request_throttle()}}
}
\concept{request-policy}
