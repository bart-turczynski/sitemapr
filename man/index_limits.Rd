% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-expansion.R
\name{index_limits}
\alias{index_limits}
\title{Default limits for sitemapindex expansion}
\usage{
index_limits(
  max_depth = getOption("sitemapr.max_index_depth", 3L),
  max_children = getOption("sitemapr.max_index_children", 50000L),
  max_total_sitemaps = getOption("sitemapr.max_total_sitemaps", Inf),
  max_total_urls = getOption("sitemapr.max_total_urls", Inf)
)
}
\arguments{
\item{max_depth}{Maximum recursion depth below the root index (integer).
Resolves from the argument, then \code{getOption("sitemapr.max_index_depth")},
then the default of 3.}

\item{max_children}{Maximum number of distinct child entries expanded per
index (integer). Resolves from the argument, then
\code{getOption("sitemapr.max_index_children")}, then the default of 50 000 (the
sitemap-protocol per-index entry limit).}

\item{max_total_sitemaps}{Maximum number of child sitemaps fetched across the
entire traversal (numeric). Resolves from the argument, then
\code{getOption("sitemapr.max_total_sitemaps")}, then the default of \code{Inf} (no
aggregate bound). Kept numeric so \code{Inf} is representable.}

\item{max_total_urls}{Maximum number of URL rows gathered across the entire
traversal (numeric). Resolves from the argument, then
\code{getOption("sitemapr.max_total_urls")}, then the default of \code{Inf} (no
aggregate bound). Kept numeric so \code{Inf} is representable.}
}
\value{
A named list of limits with coerced types.
}
\description{
Returns the configurable bounds the index-expansion engine applies while
recursively expanding a \code{sitemapindex}. Both are safety bounds, not protocol
rules: they keep a hostile or accidentally huge index tree from triggering an
unbounded burst of requests or unbounded recursion.
}
\details{
\code{max_depth} counts levels below the root index: the root index is depth 0,
its children are depth 1, and an index whose children would land beyond
\code{max_depth} is not descended (an \code{INDEX_DEPTH_EXCEEDED} event).
\code{max_children} caps how many child entries one index contributes after dedup;
entries beyond the cap are dropped (an \code{INDEX_CHILD_COUNT_EXCEEDED} event).
\code{max_total_sitemaps} and \code{max_total_urls} are TRAVERSAL-WIDE aggregate
budgets spanning the whole recursive expansion: the first caps how many child
sitemaps are fetched in total (an \code{INDEX_TOTAL_SITEMAPS_EXCEEDED} event), the
second caps how many URL rows are gathered in total (an
\code{INDEX_TOTAL_URLS_EXCEEDED} event). Both default to \code{Inf} (no aggregate
bound), preserving the historical behavior; when a budget is reached the
traversal stops and returns the accumulated PARTIAL result.
}
\examples{
index_limits()
index_limits(max_depth = 2, max_children = 1000)
index_limits(max_total_sitemaps = 500, max_total_urls = 1e6)
}
\seealso{
\code{\link[=fetch_limits]{fetch_limits()}} and \code{\link[=discovery_limits]{discovery_limits()}} for the other bound
constructors, and \code{\link[=read_sitemap]{read_sitemap()}} which accepts \code{index_limits}.
}
