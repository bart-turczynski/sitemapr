% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{sitemap_companions}
\alias{sitemap_companions}
\alias{sources}
\alias{sources.default}
\alias{sources.sitemap_audit}
\alias{problems}
\alias{problems.default}
\alias{problems.sitemap_audit}
\title{Access the sources and problems companions of a sitemap result}
\usage{
sources(x, ...)

\method{sources}{default}(x, ...)

\method{sources}{sitemap_audit}(x, ...)

problems(x, ...)

\method{problems}{default}(x, ...)

\method{problems}{sitemap_audit}(x, ...)
}
\arguments{
\item{x}{A \code{\link[=read_sitemap]{read_sitemap()}} result (a tidy tibble carrying the
\code{sources}/\code{problems} attributes) or a \code{\link[=sitemap_audit]{sitemap_audit()}} object.}

\item{...}{Ignored; reserved for future methods.}
}
\value{
For \code{sources()}, the per-source fetch-metadata records; for
\code{problems()}, the non-fatal parse \code{problems} table. A \code{\link[=sitemap_audit]{sitemap_audit()}}
object always yields the documented (possibly zero-row) component; a
\code{\link[=read_sitemap]{read_sitemap()}} result yields its attached companion table.
}
\description{
\code{sources()} and \code{problems()} return the two companion tables the package
produces alongside its tidy URL rows: the per-source fetch-metadata records
and the non-fatal parse \code{problems} table. They dispatch on the object type,
so the same call works on a \code{\link[=read_sitemap]{read_sitemap()}} result and on a
\code{\link[=sitemap_audit]{sitemap_audit()}} object:
}
\details{
\itemize{
\item On a \code{\link[=read_sitemap]{read_sitemap()}} result, they read the \code{sources} / \code{problems}
attributes the entry point attaches to the tidy tibble, so callers need not
reach for \code{attr()}.
\item On a \code{\link[=sitemap_audit]{sitemap_audit()}} object, they return the first-class \code{sources} /
\code{problems} components, delegating to \code{\link[=audit_sources]{audit_sources()}} / \code{\link[=audit_problems]{audit_problems()}}
for identical behavior.
}

The default methods return the requested attribute (or \code{NULL} when the object
carries none), so they are safe to call on any object.
}
\examples{
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  '<url><loc>https://example.com/</loc></url></urlset>'
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)
urls <- read_sitemap(path)

sources(urls)
problems(urls)

# The same accessors work on a sitemap_audit object.
audit <- sitemap_audit(urls = urls)
sources(audit)
problems(audit)
}
\seealso{
\code{\link[=audit_sources]{audit_sources()}} and \code{\link[=audit_problems]{audit_problems()}} for the \code{sitemap_audit}
component accessors.
}
