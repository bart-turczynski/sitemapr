% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit_accessors}
\alias{audit_accessors}
\alias{audit_urls}
\alias{audit_findings}
\alias{audit_sources}
\alias{audit_problems}
\alias{audit_tree}
\title{Access the components of a sitemap audit}
\usage{
audit_urls(x)

audit_findings(x)

audit_sources(x)

audit_problems(x)

audit_tree(x)
}
\arguments{
\item{x}{A \code{sitemap_audit} object, as from \code{\link[=sitemap_audit]{sitemap_audit()}}.}
}
\value{
The requested component: a tibble or data frame with the documented
columns (zero rows for an empty component).
}
\description{
Component accessors for a \code{\link[=sitemap_audit]{sitemap_audit()}} object. Each returns the stable
tidy shape the package already produces: \code{audit_urls()} the \code{\link[=read_sitemap]{read_sitemap()}}
row tibble, \code{audit_findings()} the \code{\link[=validate_sitemap]{validate_sitemap()}} findings contract,
\code{audit_sources()} the per-source fetch-metadata records, \code{audit_problems()}
the non-fatal parse \code{problems} table, and \code{audit_tree()} the \code{\link[=sitemap_tree]{sitemap_tree()}}
discovery/index structure.
}
\examples{
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  '<url><loc>https://example.com/</loc></url></urlset>'
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)
audit <- sitemap_audit(urls = read_sitemap(path))

audit_urls(audit)
audit_findings(audit)
audit_sources(audit)
audit_problems(audit)
audit_tree(audit)
}
