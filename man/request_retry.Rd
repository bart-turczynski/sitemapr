% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{request_retry}
\alias{request_retry}
\title{Retry-and-backoff settings for a request-policy}
\usage{
request_retry(
  max_tries = 3L,
  statuses = c(429L, 500L, 502L, 503L, 504L),
  backoff_min = 1,
  backoff_max = 30,
  retry_on_failure = FALSE
)
}
\arguments{
\item{max_tries}{Total attempts per hop (>= 1). \code{1} means no retry.}

\item{statuses}{Integer vector of retryable HTTP statuses. Defaults to the
transient set \verb{429, 500, 502, 503, 504}.}

\item{backoff_min, backoff_max}{Lower/upper bounds (seconds) for the
exponential backoff between attempts. The delay for attempt \code{i} is
\code{backoff_min * 2^(i - 1)}, clamped to \code{backoff_max}. A \code{Retry-After} header
is honored but likewise bounded by \code{backoff_max}, so no unbounded sleep.}

\item{retry_on_failure}{When \code{TRUE}, transient transport errors (curl
failures) are also retried. Defaults to \code{FALSE} (status-driven retry only).}
}
\value{
A \code{sitemapr_request_retry} object for \code{request_policy(retry = )}.
}
\description{
Configures bounded retry with exponential backoff for a request-policy. Pass
the result as \code{request_policy(retry = )}. Only TRANSIENT failures retry: the
retryable HTTP \code{statuses} set, plus transient transport errors when
\code{retry_on_failure = TRUE}. Deterministic failures (SSRF, malformed input,
resource-ceiling aborts) are never retried because they surface outside the
single-hop \code{req_perform()} that retry wraps.
}
\examples{
# Up to 4 attempts per hop, retrying the transient status set.
request_policy(retry = request_retry(max_tries = 4L))
}
\seealso{
Other request-policy:
\code{\link[=request_auth_basic]{request_auth_basic()}},
\code{\link[=request_auth_bearer]{request_auth_bearer()}},
\code{\link[=request_policy]{request_policy()}},
\code{\link[=request_proxy]{request_proxy()}},
\code{\link[=request_throttle]{request_throttle()}}
}
\concept{request-policy}
