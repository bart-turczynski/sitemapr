% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ruleset-context.R
\name{gsc_submission}
\alias{gsc_submission}
\title{Search Console submission context preset (ADR-009 spec 12.1)}
\usage{
gsc_submission(property)
}
\arguments{
\item{property}{The verified property/site URL the submission is bound to.
Required: it is the irreducible input and cannot be inferred. Its shape is
validated by \code{\link[=ruleset_context]{ruleset_context()}}.}
}
\value{
An object of class \code{sitemapr_ruleset_context}, the same shape
\code{\link[=ruleset_context]{ruleset_context()}} returns.
}
\description{
A construction-time preset for a Google Search Console submission. It bundles
the three per-source axes a GSC submission implies into one coherent context
so a caller need not hand-set each one: \code{submission_channel = "search_console_api"}, \code{authority_evidence = "verified_property_set"}, and
the verified \code{property_scope} the submission is bound to. It is a thin
wrapper over \code{\link[=ruleset_context]{ruleset_context()}}; every axis stays independently overridable
by calling \code{\link[=ruleset_context]{ruleset_context()}} directly.
}
\examples{
gsc_submission("https://example.com/")
}
\seealso{
\code{\link[=ruleset_context]{ruleset_context()}} for the general constructor and its axes.
}
