% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ruleset-context.R
\name{sitemap_rulesets}
\alias{sitemap_rulesets}
\title{Supported \code{sitemap_ruleset} values}
\usage{
sitemap_rulesets()
}
\value{
A character vector of the supported ruleset names, baseline first.
}
\description{
The engine axis of the ADR-009 validation context: the sitemaps.org baseline
plus the opt-in \code{google}, \code{bing}, and \code{yandex} overlays (sitemap-spec §12.0).
\code{sitemapr} owns this value set and it is extensible; this accessor is the one
canonical source vector every ruleset-aware surface validates against. The
baseline \code{"sitemaps.org"} is deliberately first so it is the default choice.
}
\examples{
sitemap_rulesets()
}
\seealso{
\code{\link[=ruleset_revision]{ruleset_revision()}} for the published revision of a ruleset and
\code{\link[=ruleset_context]{ruleset_context()}} for the per-source context axes.
}
