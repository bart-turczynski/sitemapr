% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-sitemap.R
\name{validate_sitemap_ruleset}
\alias{validate_sitemap_ruleset}
\alias{validate_sitemaps_ruleset}
\title{Validate a sitemap under an engine-aware ruleset (ADR-009)}
\usage{
validate_sitemap_ruleset(
  x,
  sitemap_ruleset = sitemap_rulesets(),
  context = ruleset_context(),
  mode = c("strict", "non-strict"),
  user_agent = default_user_agent(),
  limits = fetch_limits(),
  index_limits = NULL,
  policy = request_policy(),
  check_robots = FALSE,
  robots_user_agent = "*",
  inspect_pages = FALSE,
  page_sample = 50L,
  page_mode = c("sample", "full"),
  page_budget = page_inspection_budget(),
  page_user_agent = default_user_agent()
)

validate_sitemaps_ruleset(
  x,
  sitemap_ruleset = sitemap_rulesets(),
  context = ruleset_context(),
  mode = c("strict", "non-strict"),
  user_agent = default_user_agent(),
  limits = fetch_limits(),
  index_limits = NULL,
  policy = request_policy(),
  check_robots = FALSE,
  robots_user_agent = "*",
  inspect_pages = FALSE,
  page_sample = 50L,
  page_mode = c("sample", "full"),
  page_budget = page_inspection_budget(),
  page_user_agent = default_user_agent()
)
}
\arguments{
\item{x}{One or more sitemap URLs or paths to local sitemap files (\code{.xml},
\code{.txt}, \code{.gz}, or \code{.tar.gz}).}

\item{sitemap_ruleset}{The engine ruleset to validate under; one of
\code{\link[=sitemap_rulesets]{sitemap_rulesets()}} (baseline \code{"sitemaps.org"} first, the default). The
baseline emits the schema-v1 result; an engine overlay adds the additive
columns.}

\item{context}{A per-source validation context from \code{\link[=ruleset_context]{ruleset_context()}} (the
four independent ADR-009 §1 axes). Carried into the \code{context} list-column
of the additive result. Ignored on the baseline path (which emits no
additive columns).}

\item{mode}{\code{"strict"} (the default) or \code{"non-strict"}. In \code{non-strict},
strict-only findings are dropped and schema violations are downgraded to
\code{warning}; in \code{strict}, the documented info-to-warning codes are elevated.}

\item{user_agent}{The User-Agent header for HTTP fetches. Defaults to the
package User-Agent.}

\item{limits}{Network limits for HTTP fetches, as from \code{fetch_limits()}.}

\item{index_limits}{Sitemapindex-expansion bounds (recursion depth and
per-index child-count cap), as from \code{index_limits()}. Defaults to
\code{index_limits()}.}

\item{policy}{A \code{\link[=request_policy]{request_policy()}} applied to every HTTP hop (root,
robots.txt, discovery, redirects, and index children) — configure custom
headers, authentication, a proxy, TLS options, retry/backoff, and per-host
throttling there. Defaults to the no-op policy.}

\item{check_robots}{Logical; when \code{TRUE}, run the robots.txt allow/disallow
check over the advertised URLs (requires the optional \code{robotstxtr}
package). Defaults to \code{FALSE}.}

\item{robots_user_agent}{The robots.txt group to match against when
\code{check_robots = TRUE}, e.g. \code{"*"} (the catch-all group, the default) or a
specific crawler token such as \code{"Googlebot"}.}

\item{inspect_pages}{Logical; the master opt-in for per-URL page inspection
(Layer E). When \code{FALSE} (the default) no page is fetched and the result is
byte-identical to a call without it: the pinned ten-column findings surface
and no \code{page_coverage} attribute. When \code{TRUE}, a budgeted, deduplicated,
deterministically-sampled set of the advertised page URLs is fetched and
each fetch's transport outcome maps to at most one \code{page}-layer finding
(\code{PAGE_STATUS_ERROR}, \code{PAGE_STATUS_REDIRECT}, \code{PAGE_REDIRECT_CHAIN},
\code{PAGE_FETCH_FAILED}, \code{PAGE_SSRF_BLOCKED}); the run's coverage rides the
\code{page_coverage} attribute (see Value). Network expansion is never
implicit. Page inspection is batch-wide: one budget over the union of the
call's deduped page URLs.}

\item{page_sample}{Integer sample size for \code{page_mode = "sample"}: how many
of the deduplicated page URLs to inspect, chosen by a deterministic stable
hash order so re-runs pick the same set. Ignored when \code{page_mode = "full"}.}

\item{page_mode}{\code{"sample"} (inspect \code{page_sample} deduplicated URLs, the
default) or \code{"full"} (inspect every deduplicated URL, subject to the
budget caps).}

\item{page_budget}{A page-inspection budget list: the aggregate caps (max
pages, max requests/hops, max aggregate bytes, per-page body cap, max wall
time), each caller-overridable with a safe default. Applies only when
\code{inspect_pages = TRUE}.}

\item{page_user_agent}{The HTTP request User-Agent sent when fetching pages
(recorded for the "what did the inspector see" caveat; distinct from a
robots product token). Defaults to sitemapr's inspector UA.}
}
\value{
The findings tibble of \code{\link[=validate_sitemap]{validate_sitemap()}}. Under the baseline
\code{sitemap_ruleset} it is exactly the pinned ten columns; under an engine
overlay it additionally carries \code{ruleset} (character), \code{ruleset_revision}
(character), \code{context} (a list-column of the context object as a named
list), and \code{provenance} (character, per finding), appended in that order
after the ten pinned columns. The same source, mode, ruleset, and context
yield a row-for-row identical tibble across calls.
}
\description{
The versioned, engine-aware entry point parallel to \code{\link[=validate_sitemap]{validate_sitemap()}}. It
runs the identical validation pipeline (the XSD schema, protocol/semantic,
byte-level classification, and bounded index-expansion layers), then, when an
engine overlay is selected, augments the findings tibble with the additive
schema-v2 columns (\code{docs/decisions/ADR-009-per-engine-validation-profiles.md}
§5/§6, \code{docs/findings-contract.md} "Per-engine ruleset extension").
}
\details{
Backwards compatibility is preserved by construction (ADR-009 §5): a baseline
call (\code{sitemap_ruleset = "sitemaps.org"}, the default) returns exactly the
pinned ten-column schema-v1 result, byte-identical to \code{\link[=validate_sitemap]{validate_sitemap()}}.
The four additive columns appear \strong{only} for an explicit engine overlay
(\code{"google"} / \code{"bing"} / \code{"yandex"}); there is deliberately no \verb{profile=}
argument on \code{\link[=validate_sitemap]{validate_sitemap()}} and no silent default switch to an engine.

This slice fixes the engine-aware carrier and the additive schema; no
per-engine evaluators exist yet, so every finding produced under an overlay
is a reused baseline code and carries \code{provenance = "inherited_protocol"} (an
ADR-009 §0 executable class). Later slices override the provenance per code.

Per-URL page inspection (\code{inspect_pages = TRUE}) works exactly as in
\code{\link[=validate_sitemap]{validate_sitemap()}}, except the transport / canonical / hreflang page
findings are assembled under the selected \code{sitemap_ruleset} too: under an
engine overlay they carry the same additive schema-v2 columns as the base
findings, so the per-engine provenance / context (ADR-009 §5.2/§5.3) engages
over the page layer rather than emitting as generic baseline diagnostics. As
in \code{\link[=validate_sitemap]{validate_sitemap()}}, \code{inspect_pages = FALSE} is byte-identical to a call
without the argument.
}
\examples{
xml <- paste0(
  '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',
  '<url><loc>https://example.com/</loc></url>',
  '</urlset>'
)
path <- tempfile(fileext = ".xml")
writeLines(xml, path)

# Baseline: identical to validate_sitemap().
validate_sitemap_ruleset(path, "sitemaps.org")

# Engine overlay: adds the additive schema-v2 columns.
validate_sitemap_ruleset(path, "google")
}
\seealso{
\code{\link[=validate_sitemap]{validate_sitemap()}} for the baseline entry point,
\code{\link[=sitemap_rulesets]{sitemap_rulesets()}} for the ruleset value set, and \code{\link[=ruleset_context]{ruleset_context()}} for
the per-source context axes.
}
