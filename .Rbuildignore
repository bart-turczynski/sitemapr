# R build/dev artifacts
^README\.Rmd$
^_pkgdown\.yml$
^site$
^.*\.Rproj$
^\.Rproj\.user$
^\.Rhistory$
^\.RData$
^\.Ruserdata$
^.*\.Rcheck$
^[^/]+\.tar\.gz$
^\.lintr$
^cran-comments\.md$
^CRAN-SUBMISSION$
^doc$
^Meta$
# Shared scaffold layer: everything below is dev tooling, not package content,
# and would otherwise trip "non-standard files/directories" in R CMD check.
^\.editorconfig$
^\.env$
^\.env\.example$
^\.gitattributes$
^\.gitignore$
^\.gitlab-ci\.yml$
# The GitLab CI dependency library (SITE-dzikrmnh). Cache paths must live
# inside CI_PROJECT_DIR, so pak's library sits in the package directory and
# `R CMD build` would otherwise sweep it into the tarball -- a 69 MB tarball
# plus a hidden-files NOTE, a portable-file-names NOTE and an
# executable-files WARNING, all from someone else's installed packages, not
# sitemapr's own content. Ported from pagerankr's `.Rbuildignore`.
^\.cache$
^\.pre-commit-config\.yaml$
^air\.toml$
^\.fp$
^\.claude$
^tools$
^FP_CLAUDE\.md$
^FP_AGENTS\.md$
^_scratch$
^data-raw$
^docs$
^CLAUDE\.md$
^AGENTS\.md$
^CONTRIBUTING\.md$
^CODE_OF_CONDUCT\.md$
^SECURITY\.md$
^CHANGELOG\.md$
^CITATION\.cff$
^codemeta\.json$
^\.zenodo\.json$
^\.bestpractices\.json$
^\.DS_Store$
^ACKNOWLEDGMENTS\.md$
# The citation gate ported from seor (design/adr/0001 and 0002 there).
# Not part of the installable package; without this, --as-cran reports it
# as a non-standard top-level directory.
^scripts$
